<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Actualizar Usuario</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #7abddd;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            width: 350px;
            text-align: center;
        }

        h2 {
            color: #333;
        }

        label {
            font-weight: bold;
            display: block;
            margin-top: 10px;
            color: #555;
        }

        input, textarea, select {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 14px;
            transition: 0.3s ease;
        }

        input:focus, textarea:focus, select:focus {
            border-color: #7abddd;
            outline: none;
            box-shadow: 0 0 5px rgba(122, 189, 221, 0.5);
        }

        button {
            background-color: #7abddd;
            color: #ffffff;
            border: none;
            padding: 10px;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            margin-top: 15px;
            width: 100%;
            transition: 0.3s;
        }

        button:hover {
            background-color: #5ea4c8;
        }

        p {
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Actualizar Datos de Usuario</h2>

        <% if (mensaje) { %>
            <p style="color: green;"><%= mensaje %></p>
        <% } %>

        <form action="/api/auth/discord/update-user" method="POST">
            <label>User ID:</label>
            <input type="text" name="userid" required>

            <label>Security Token:</label>
            <input type="text" name="token" required>

            <label>Nombre:</label>
            <input type="text" name="name" value="<%= user ? user.name : '' %>">

            <label>Apodo:</label>
            <input type="text" name="nickname" value="<%= user ? user.nickname : '' %>">

            <label>Edad:</label>
            <input type="number" name="age" min="13" value="<%= user ? user.age : '' %>">

            <label>Descripción:</label>
            <textarea name="description"><%= user ? user.description : '' %></textarea>

            <label>Género:</label>
            <select name="gender">
                <option value="male" <%= user && user.gender === "male" ? "selected" : "" %>>Masculino</option>
                <option value="female" <%= user && user.gender === "female" ? "selected" : "" %>>Femenino</option>
                <option value="other" <%= user && user.gender === "other" ? "selected" : "" %>>Otro</option>
                <option value="prefer_not_to_say" <%= user && user.gender === "prefer_not_to_say" ? "selected" : "" %>>Prefiero no decirlo</option>
            </select>

            <button type="submit">Actualizar</button>
        </form>
    </div>
</body>
</html>
